# Backend connection (Socket.IO + REST). The frontend reads these at build time
# via Vite's import.meta.env. Copy this file to `.env` and adjust as needed.
VITE_SOCKET_URL=http://localhost:3000
VITE_API_URL=http://localhost:3000/api
